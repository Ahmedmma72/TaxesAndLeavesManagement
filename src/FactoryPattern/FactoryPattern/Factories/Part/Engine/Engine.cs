namespace FactoryPattern.Factories.Part.Engine
{
    // Step 4: Define concrete classes for each type of engine, body, and interior.

    abstract class Engine
    {
        public abstract string GetDescription();
    }
}